<%= render :partial=>"left_menu" %>
	<div class="span9">
          <div class="hero-unit">
	    <% if flash[:notice] %>	
	      <div class="alert alert-success">
		<a class="close" data-dismiss="alert" href="#">×</a>
		<h4 class="alert-heading">Success!</h4>
		<%= h(flash[:notice]) %>
	      </div>
	    <% end %>
	      
            <% if @task %>
	      <h1><%= @task.name %></h1>
	      <p>Deadline is <strong><%#= @task_main.deadline.strftime("%d/%m/%Y") %></strong></p>
	      <%= simple_form_for @task, :url => { :action => :update, :id =>  @task.id  }, :html => { :class => 'well' } do |f| %>
		<%= f.submit 'Check as done', :class=>'btn btn-success btn-large' %>
		<%= f.submit 'Edit', :class=>'btn btn-warning btn-large' %>
		<%= f.submit 'Cancel task', :class=>'btn btn-danger btn-large' %>
	      <% end %>
	    <% else %>
	      <h1>I've not any tasks!</h1>
	    <% end %>
          </div>

<% if can? :create, Task %>
	asd
<% end %>

		<p><h1>Other tasks &darr;</h1></p>
		
		<div class="row-fluid">
			<%# @tasks.each_with_index do |task, index| %>
		        <div class="span4">
		          <h2>asd<%#= task.name %></h2>
		          <p>asd<%#= task.text %></p>
		          <p>asd<a class="btn" href="#">View details &raquo;</a></p>
		        </div>
			<%# if (index+1)%3==0 %>
				</div><div class='row-fluid'>
			<%# end %>
			<%# end %>
		</div>

		<%#= will_paginate(@tasks) %>


        </div><!--/span-->
      </div><!--/row-->
